let numbero = prompt("Ingresa un número: ");
if(!isNaN(numbero) && numbero > 0)
{
  Sumador(numbero)
}
function Sumador(numberito)
{
  for (let i = 1; i <= numberito; i++) {
    console.log(i)
    
  }
}
